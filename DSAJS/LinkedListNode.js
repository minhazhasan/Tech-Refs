// Linked List node class
// value = node value
// next = next node pointer from the current node

export class LinkedListNode {
  constructor(value, next = null) {
    this.value = value;
    this.next = next;
  }
}
